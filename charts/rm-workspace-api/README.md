# Helm chart for the Workspace API (v2)

## Installing the Chart

You can install the chart with the release name `rm-workspace-api` in `default` namespace as below.

```bash
$ helm install workspace-api charts/rm-workspace-api
```